var url = require('url');
var querystring = require('querystring');

// Given a url return the parsed query parameters
function getQueryParameters(rawUrl) {
  var parsed = url.parse(rawUrl);
  return querystring.parse(parsed.query);
}

module.exports = getQueryParameters;
