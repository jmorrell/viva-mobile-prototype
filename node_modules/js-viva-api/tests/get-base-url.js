var url = require('url');

// Given a url return the parsed query parameters
function getBaseUrl(rawUrl) {
  var parsed = url.parse(rawUrl);
  return parsed.protocol + '//' + parsed.host + parsed.pathname;
}

module.exports = getBaseUrl;
